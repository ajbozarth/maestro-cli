apiVersion: maestro/v1
kind: Workflow
metadata:
  name: Openai agent test workflow
  labels:
    app: test1
spec:
  template:
    metadata:
      name: openai_test
      labels:
        app: my-agent
    agents:
        - my-agent
    prompt: Say a funny story about dogs and cats.
    steps:
      - name: step1
        agent: my-agent

